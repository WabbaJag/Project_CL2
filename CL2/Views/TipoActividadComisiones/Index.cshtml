@model IEnumerable<CL2.Models.TipoActividadComision>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DetalleActividadComision)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DetalleActividadComision)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TipoActividadComisionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoActividadComisionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoActividadComisionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
